import streamlit as st

host = st.secrets["host"]
port = st.secrets["PORT"]
database = st.secrets["HMS"]
username = st.secrets["admin"]
password = st.secrets["HANUMAN2004"]
mongodb_password = st.secrets["hanuman2004"]
